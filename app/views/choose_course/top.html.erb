
<p>
    問題を選択(現在は五択問題のみ)
    <%= form_tag(course_path, method: :get) do  %>
        <%= select_tag 'num', options_for_select(Label.all.map{|n| n.id.to_s()}) %>

    <%= submit_tag("問題へ") %>
    <% end %>
    

</p>